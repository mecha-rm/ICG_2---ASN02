#include "ITexture.h"
#include <glad\glad.h>

// returns the internal format
// cherry::InternalFormat cherry::ITexture::GetInternalFormat() const
// {
// 	return myInternalFormat;
// }

// binds the texture to a slot.
// void cherry::ITexture::Bind(uint32_t slot)
// {
// 	glBindTextureUnit(slot, myRendererID);
// }
